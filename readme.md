Application based on the laravel framework.

Datenbank installieren:
a) Benutzer anlegen (siehe application/config/database.php)
b) php artisan migrate:install
c) php artisan migrat

Datenbank installieren:
a) Benutzer anlegen (siehe application/config/database.php)
b) php artisan migrate:install
c) php artisan migrate
